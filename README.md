# RV32I_CPU
This project presents the design and implementation of a 5-stage pipelined RISC-V CPU based on the RV32I instruction set. The processor is developed using Verilog HDL and follows a classical pipeline architecture consisting of Instruction Fetch (IF), Instruction Decode (ID), Execute (EX), Memory (MEM), and Write Back (WB) stages.
